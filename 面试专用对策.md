# BaykeShop 电商项目面试专用对策

---

## 目录
- [技术栈总览](#技术栈总览)
- [简历描述模板](#简历描述模板)
- [核心面试题 Q1-Q8（基础层）](#核心面试题-q1-q8基础层)
- [进阶面试题 Q9-Q17（深度层）](#进阶面试题-q9-q17深度层)
- [高频追问 Q18-Q25（概念原理层）](#高频追问-q18-q25概念原理层)
  - [Q18: 死锁 & 分布式锁](#q18-什么是死锁什么是分布式锁)
  - [Q19: 进程池 & 线程池](#q19-进程池和线程池是什么项目用不用好处和坏处)
  - [Q20: Redis 缓存机制](#q20-redis-的缓存机制是怎么运行的)
  - [Q21: 秒杀功能实现](#q21-秒杀功能怎么实现如何防止超卖出现超卖怎么解决)
  - [Q22: 悲观锁 & 乐观锁](#q22-悲观锁和乐观锁是什么区别和作用)
  - [Q23: IP 验证的坑](#q23-ip-验证这块有什么坑)
  - [Q24: Linux 常用命令](#q24-linux-常用命令及其用处)
  - [Q25: Docker 常用命令](#q25-docker-常用命令及其用处)
- [通用基础题 Q26-Q35（补充层）](#通用基础题-q26-q35补充层)
  - [Q26: GIL 是什么](#q26-gil-是什么对多线程有什么影响)
  - [Q27: 装饰器是什么](#q27-装饰器是什么有什么作用)
  - [Q28: 深拷贝 vs 浅拷贝](#q28-深拷贝-vs-浅拷贝)
  - [Q29: __new__ vs __init__](#q29-__new__-vs-__init__-的区别)
  - [Q30: TCP 三次握手/四次挥手](#q30-tcp-三次握手四次挥手)
  - [Q31: HTTP vs HTTPS](#q31-http-vs-https-的区别)
  - [Q32: HTTP 状态码](#q32-http-常见状态码)
  - [Q33: 跨域问题](#q33-跨域问题怎么解决)
  - [Q34: 索引是什么](#q34-索引是什么为什么能加速查询)
  - [Q35: 事务的四个特性](#q35-事务的四个特性-acid)
- [项目部署 & Docker 化](#项目部署--docker-化)
- [面试自信话术](#面试自信话术)
- [补充面试题 Q36-Q90（高频八股防守层）](#补充面试题-q36-q90高频八股防守层)
  - [Q36: args/kwargs](#q36args-和-kwargs-是什么)
  - [Q37: 生成器vs迭代器](#q37生成器和迭代器有什么区别)
  - [Q38: 闭包](#q38闭包是什么有什么坑)
  - [Q39: classmethod/staticmethod](#q39classmethodstaticmethod-和实例方法有什么区别)
  - [Q40: Django MVT](#q40django-的-mvt-是什么请求生命周期是怎样的)
  - [Q41: 中间件](#q41django-中间件是什么怎么写一个自定义中间件)
  - [Q42: DRF认证权限](#q42drf-的认证和权限机制是怎样的)
  - [Q43: Signal](#q43django-signal-是什么适用场景有哪些)
  - [Q44: select_related/prefetch_related](#q44select_related-和-prefetch_related-的区别和使用场景)
  - [Q45: 元组vs列表](#q45元组和列表有什么区别什么时候用元组)
  - [Q46: Redis数据类型](#q46redis-支持哪些数据类型各自适用什么场景)
  - [Q47: MVT vs MVC](#q47mvt-和-mvc-有什么区别)
  - [Q48: Django请求流程](#q48django-请求的执行流程是怎样的)
  - [Q49: 单例模式](#q49python-怎么实现单例模式)
  - [Q50: 作用域](#q50python-变量的作用域有哪些)
  - [Q51: Linux命令](#q51生产环境最常用的-10-个-linux-命令是什么)
  - [Q52: 参数传递](#q52python-函数参数是值传递还是引用传递)
  - [Q53: 垃圾回收](#q53python-的垃圾回收机制和内存管理是怎样的)
  - [Q54: 内存泄露](#q54遇到过内存泄露吗怎么解决的)
  - [Q55: 线程池](#q55线程池的工作原理是什么)
  - [Q56: 异常处理](#q56python-的异常处理机制是怎样的)
  - [Q57: 上下文管理器](#q57什么是上下文管理器with-语句的原理)
  - [Q58: 魔法方法](#q58python-常用魔法方法有哪些)
  - [Q59: Mixin](#q59什么是-mixin-模式)
  - [Q60: CSRF](#q60什么是-csrf-攻击django-怎么防护)
  - [Q61: ORM聚合](#q61django-orm-怎么做聚合查询)
  - [Q62: 数据库路由](#q62django-怎么实现读写分离)
  - [Q63: 自定义命令](#q63怎么写-django-自定义管理命令)
  - [Q64: 缓存版本](#q64django-缓存怎么设置过期时间和版本控制)
  - [Q65: 事务隔离](#q65数据库事务隔离级别有哪些各有什么问题)
  - [Q66: 连接池](#q66什么是数据库连接池)
  - [Q67: Session/Cookie/Token](#q67sessioncookie-和-token-有什么区别)
  - [Q68: JWT](#q68jwt-是什么结构是怎样的)
  - [Q69: XSS/SQL注入](#q69怎么防止-xss-和-sql-注入)
  - [Q70: 工厂模式](#q70什么是工厂模式)
  - [Q71: 性能优化](#q71项目做过哪些性能优化)
  - [Q72: 线上故障](#q72遇到过线上故障吗怎么排查的)
  - [Q73: 日志监控](#q73项目怎么做日志和监控)
  - [Q74: 多进程](#q74python-多进程和多线程怎么选)
  - [Q75: 异步IO](#q75什么是异步-ioasyncio-的原理)
  - [Q76: 支付宝RSA2](#q76支付宝支付的-rsa2-签名流程是怎样的)
  - [Q77: 支付幂等性](#q77怎么保证支付订单的幂等性)
  - [Q78: 同步异步回调](#q78支付宝的同步回调和异步回调有什么区别)
  - [Q79: 重复支付](#q79怎么防止重复支付)
  - [Q80: Celery路由](#q80celery-怎么做任务路由和优先级队列)
  - [Q81: Celery监控](#q81怎么监控-celery-任务的执行情况)
  - [Q82: Celery死信](#q82什么是死信队列怎么配置)
  - [Q83: RESTful规范](#q83restful-api-的设计规范有哪些)
  - [Q84: API版本](#q84api-版本控制有哪些方案)
  - [Q85: Django测试](#q85django-怎么做单元测试和接口测试)
  - [Q86: Mock测试](#q86什么是-mock-测试中怎么用)
  - [Q87: Git工作流](#q87git-常用工作流有哪些你们用什么)
  - [Q88: rebase](#q88git-rebase-和-merge-有什么区别)
  - [Q89: 冲突解决](#q89git-冲突怎么解决)
  - [Q90: 代码审查](#q90代码审查review-看哪些方面)
- [附录：Docker 与部署进阶](#附录docker-与部署进阶)
- [附录：PostgreSQL 进阶](#附录postgresql-进阶)

---

## 技术栈总览

### 核心框架
- **后端框架**: Django 4.2.17（LTS 版本）
- **API 框架**: Django REST Framework 3.16.1
- **包装**: baykeshop 1.3.25（完整开源电商包）

### 数据库 & 缓存
- **关系数据库**: PostgreSQL（shop_db）
- **缓存**: Redis 7.3.0（3个不同的数据库）
  - db0: 默认缓存和 Session 存储
  - db1: 业务缓存（商品、购物车等）
  - db2: Celery 任务队列
  - db3: Celery 结果存储

### 异步任务
- **Celery 5.3.4**: 异步任务处理（邮件、SMS 验证）
- **Kombu 5.6.2**: 消息代理
- **Eventlet 0.40.4**: 异步并发支持

### 支付集成
- **支付宝**: alipay-sdk-python 3.7.1018（RSA2 验签）
- **加密库**: pycryptodome 3.23.0, rsa 4.9.1

### 其他关键库
- **图片处理**: Pillow 12.1.1
- **数据可视化**: pyecharts 2.1.0
- **日志**: loguru

---

## 简历描述模板

### 项目名称
**BaykeShop 开源电商系统（Django + PostgreSQL + Redis + Celery）**

### 项目描述
基于 Django 4.2 和 baykeshop 二次开发的全功能电商平台，包含商品管理、购物车、订单支付、邮箱验证、后台管理等完整模块。使用 PostgreSQL 存储业务数据，Redis 实现缓存和会话管理，Celery 处理异步邮件任务。

### 技术亮点
1. **PostgreSQL 深度应用**：用 JSONB 存储用户 IP 白名单，支持灵活扩展；使用 F() 表达式实现原子性库存扣减，防止超卖
2. **Redis 多级缓存**：商品详情缓存（TTL 30分钟）、购物车缓存、用户 Session，并设计缓存击穿、穿透、雪崩三防方案
3. **IP 白名单安全机制**：基于 Redis 和 PostgreSQL 的会话验证，支持 IP 变动检测和登出
4. **Celery 异步邮件**：邮箱验证码异步发送，失败重试机制，信号钩子统一记录失败日志

### 关键难点及解决方案
1. **防止重复支付**：支付宝回调加幂等性校验，订单状态机（待支付→已支付→已取消）防止状态异常
2. **缓存一致性问题**：商品更新后删除缓存，而非更新缓存，避免脏数据
3. **Session 分布式**：改用 Redis 存储 Session，支持多实例部署
4. **API 权限控制**：DRF Token 认证 + IP 白名单双重校验，确保数据安全

---

## 核心面试题 Q1-Q8（基础层）

---

### Q1：电商库存怎么设计？如何防止超卖？

**面试话术**：

"库存设计这块，我分了数据库设计、库存初始化、库存预警、防超卖四个方面来说。

**数据库设计**：商品表有个库存字段 `stock`，存的是剩余库存。另外订单表每个订单也存了购买数量，方便追溯和统计。

**库存初始化**：商品上架的时候，后台管理员输入初始库存，比如 100 件，直接写到数据库。上架之后，库存数据就进入流转状态，有下单扣减、取消订单回退、后台补货等操作。

**库存预警**：我做了个定时任务，每天检查一次库存，如果某个商品的库存低于阈值，比如只剩 10 件，就发邮件提醒管理员补货。避免用户下单的时候发现库存不足，体验不好。

**防超卖**：这块我用两层防护。

第一层是数据库层面的，用原子操作扣库存。就是直接在数据库里执行「库存减数量」这个操作，而不是先读出来、减了再写回去。用 Django ORM 的 F 表达式实现，比如 `Product.objects.filter(id=1).update(stock=F('stock') - 1)`。即使 100 个请求同时来扣同一个商品的库存，数据库也会保证每次只扣一次，不会出现负数。

第二层是高并发场景，比如秒杀。这时候流量很大，数据库扛不住，我会先用 Redis 预扣库存。Redis 操作比数据库快很多，而且可以用 `decr` 命令保证原子性。Redis 扣成功了，再异步创建订单和扣数据库库存。这样前端响应快，用户体验好，后端压力也小。

**库存回滚**：如果 Redis 扣了库存但创建订单失败，比如支付超时或者用户取消，我要把库存加回去。我在订单取消或者支付失败的时候，手动触发回滚操作，给 Redis 加库存。另外为了保险，我会定期检查 Redis 和数据库的库存是否一致，如果不一致就同步。"

---

### Q2：支付宝支付是怎么集成的？如何防止重复支付？

**面试话术**：

"支付宝集成整个流程是这样的：

**第一步：创建支付请求。** 用户点支付按钮，后端根据订单 SN 和支付金额，调用支付宝 SDK 生成支付链接，返回给前端，前端跳转到支付宝页面让用户付款。这一步不是阻塞等待，是直接拿到 URL 就返回了。

**第二步：等待支付宝异步回调。** 用户在支付宝页面付款完成后，支付宝会主动 POST 请求到我配置的回调接口（notify_url），把支付结果、订单号、金额、签名这些参数传过来。注意这里有两套回调要区分：return_url 是页面跳转用的，不可靠；notify_url 是支付宝服务器主动通知的，这个才是真正改订单状态的地方。

**第三步：验签。** 收到回调先验签，用支付宝公钥去验证回调里的签名，确认这个请求真的是支付宝发来的，不是别人伪造的。验签不过直接丢弃。

**第四步：业务校验。** 验签通过之后，还要再验三样东西：订单号是不是我系统里存在的、金额对不对得上、支付状态是不是 TRADE_SUCCESS。防止有人篡改金额或者伪造订单。

**第五步：幂等处理 + 更新状态。** 先查订单状态，如果已经是已支付就直接返回 success，不做重复处理。如果是待支付，就改成已支付，同时扣减库存。

**第六步：返回 success。** 处理完之后必须返回纯文本 success 给支付宝，告诉它我收到了，否则支付宝会一直重试，最多重试 8 次。"

**追问：支付集成有什么坑？**
> "坑主要有几个。第一，回调接口不能加登录验证，因为是支付宝服务器来调的，没有 session 和 token，加了会 403。第二，金额要用 Decimal 处理，不能用浮点数，会有精度问题。第三，回调接口要快速响应，复杂逻辑丢给 Celery 异步处理，接口只做验签、改状态、返回 success。第四，沙箱环境和生产环境的密钥不一样，上线前必须切换，忘了换会导致支付失败。第五，return_url 和 notify_url 要分开，return_url 只是跳转页面，notify_url 才是真正处理业务逻辑的地方。"

---

### Q3：邮箱验证功能怎么实现的？为什么用 Celery？

**面试话术**：

"邮箱验证流程是这样的：用户输入邮箱，后端生成一个6位的随机验证码，存到 Redis 里，设置5分钟过期。然后把发送邮件的任务放到队列里，用户马上就能收到响应说验证码已发送。

为什么用 Celery？因为发邮件是个慢操作，可能要几百毫秒甚至更久。如果在主线程里发，用户要一直等邮件发完才能继续操作，体验很差。用了 Celery 异步之后，用户点发送按钮马上就看到「验证码已发送」的提示，后端在后台慢慢发邮件。

如果 Celery 挂了怎么办？我配了重试机制，任务失败会自动重试3次，间隔时间一次比一次长。另外我也可以通过任务ID去查任务状态，如果一直没执行成功，就发个告警。"

---

### Q4：IP 白名单是怎么实现的？有什么作用？

**面试话术**：

"IP 白名单是我在用户表里加了一个字段，用来存用户信任的 IP 列表。每次用户登录或者访问受保护接口的时候，我会先拿到用户的真实 IP 地址，然后去查一下这个 IP 在不在他的白名单里。如果不在，就不让登录或者直接登出。

获取真实 IP 的时候要注意，如果用了反向代理或者 CDN，直接取的 IP 可能是代理服务器的 IP，不是用户真实的。这时候要从请求头里拿，但要注意客户端是可以伪造这个请求头的，所以要做一些校验，比如只取最右边那个 IP（离我们最近的一跳）。

这个功能的作用是防止账号被盗后异地登录。如果有人偷了密码但 IP 不在白名单里，他登录不上去。结合 Session 机制，如果发现 IP 变了，说明可能有问题，我就强制让用户重新登录。

如果用户换了 WiFi 或者从 4G 换到 WiFi，IP 肯定会变。这是个 trade-off，安全性 vs 便利性。我的方案是 IP 变了就登出，用户重新登录就行。如果希望更友好，可以加个设备指纹，比如用 User-Agent 加 Cookie 来识别设备，允许 IP 在一定范围内变动。"

---

### Q5：缓存三防（击穿、穿透、雪崩）怎么设计？

**面试话术**：

"缓存这三个问题我分开来说。

**缓存击穿**：热点 Key 突然失效，大量请求同时打到数据库，把数据库压垮。我的方案是加分布式锁。缓存失效之后，只让第一个拿到锁的请求去查数据库重建缓存，其他请求等一下再读缓存。这样数据库只被查一次，不会雪崩式涌入。

**缓存穿透**：有人一直查一个根本不存在的数据，每次都穿透缓存打到数据库，数据库白跑一趟。我的方案是缓存空值。查到数据库也没有，就在 Redis 里缓存一个空字符串，TTL 设短一点比如 5 分钟。下次再查，直接从缓存里拿到空值就返回了，不走数据库。

**缓存雪崩**：大量 Key 同时失效，所有请求瞬间都去查数据库，数据库直接炸。我的方案是给每个 Key 的过期时间加一个随机偏移量，比如 30 分钟加减一个随机的几分钟。这样 Key 不会集中在同一时刻失效，请求就被分散开了。

我的项目里商品详情缓存用了这三防，TTL 是 1800 加随机 60 到 300 秒。"

**追问：Bloom Filter 知道吗？能解决什么问题？**
> "Bloom Filter 是一个概率型数据结构，可以快速判断一个元素是否在集合里。占用空间很小，判断速度快，但有一定误判率。在缓存场景下，如果 Bloom Filter 说某个 Key 不存在，那就真的不存在，不用去查数据库，直接返回空。这样能拦住大量的无效查询，彻底解决缓存穿透问题。但如果它说存在，还是要去查一下，因为有误判率。"

---

### Q6：API 权限是怎么控制的？

**面试话术**：

"API 权限我用了两层控制。

第一层是 Token 认证。用户登录之后，生成一个 Token 存在 Redis 里，有效期7天。用户每次请求都要在请求头里带上这个 Token，后端拿到 Token 之后，去 Redis 里查一下是不是有效的，确认用户身份。

第二层是 IP 白名单校验。对于一些敏感的接口，除了验证 Token，还会检查用户的 IP 地址是不是在他的白名单里。如果 IP 不在白名单，即使 Token 是对的，也不让访问。

Token 怎么保证安全？我把它存在 Redis 里，设置7天过期。每次请求都会验证 Token 是否还有效。用户登出的时候，我会把 Redis 里的 Token 删掉。另外，Token 只放在请求头里，不会放在 URL 里，避免被日志记录下来泄露出去。"

---

### Q7：订单状态机怎么设计的？

**面试话术**：

"订单状态机我设计了5个状态：待支付、已支付、已发货、已完成、已取消。

状态流转规则是这样的：用户下单之后是待支付，付完钱变成已支付，商家发货变成已发货，用户确认收货或者7天自动确认变成已完成。在变成已支付之前，用户可以取消订单，直接变成已取消。另外，待支付超过30分钟还没支付，定时任务会自动把它改成已取消。

我会在代码里限制状态只能按规则流转，不能随便跳。比如，不能从待支付直接变成已发货，也不能从已支付变成待支付。

如果用户付了款但订单状态没更新怎么办？支付宝的回调接口做了幂等处理，即使回调多次，也只会把待支付改成已支付，不会重复处理。如果回调真的失败了，我可以手动触发补单接口，用支付宝的交易号去查真实的支付状态，然后更新订单。另外，我还有个定时任务，每5分钟查一次待支付订单，超过30分钟未支付的就自动取消。"

---

### Q8：Session 分布式怎么处理？

**面试话术**：

"默认情况下，Django 的 Session 是存在文件或者数据库里的。如果部署多个实例，比如两个 Web 服务器，用户的请求可能被负载均衡分到不同的服务器上，这时候 Session 就不一致了，比如用户在服务器A登录了，下次请求分到服务器B，服务器B没有这个 Session，用户就被踢下线了。

解决办法是把 Session 存到 Redis 里。我配置了 Django 使用 Redis 作为 Session 的存储后端，所有 Web 服务器都连同一个 Redis。这样无论用户的请求被分到哪个服务器，都能从 Redis 里拿到 Session，保证一致。

Session 数据量大怎么办？可以分片，比如按用户 ID 的 hash 值分到不同的 Redis 实例上。另外，Session 也有过期时间，Redis 的 TTL 机制会自动清理过期的 Session，不会一直堆积。"

---

## 进阶面试题 Q9-Q17（深度层）

---

### Q9：为什么用 PostgreSQL 而不是 MySQL？

**面试话术**：

"我选 PostgreSQL 主要有三个原因。

第一是 JSONB 支持更好。我的用户表有个字段存 IP 白名单，用的是 JSONB 类型，可以直接用 PostgreSQL 的 JSON 操作符做包含查询。MySQL 虽然也有 JSON 类型，但查询性能和功能不如 PostgreSQL。

第二是并发性能更好。PostgreSQL 的 MVCC 实现不用 gap lock，高并发下锁竞争更少。我的库存扣减用 F 表达式做原子更新，PostgreSQL 执行效率更高。

第三是复杂查询支持更强。PostgreSQL 支持 CTE、窗口函数、全文检索，如果以后有复杂的数据分析需求扩展起来更方便。

MySQL 也有优势，比如简单查询更快、复制和集群方案更成熟、社区更大。所以如果项目是简单 CRUD，MySQL 够用；如果有复杂查询、JSON 操作、并发要求高，PostgreSQL 更合适。我的项目两个点都用到了，所以选了 PostgreSQL。"

**追问：MySQL 的 InnoDB 是怎么实现 MVCC 的？**
> "InnoDB 用 undo log 和 read view 实现快照读。每个事务开始时生成 read view，后续查询时根据 undo log 找到对应版本的数据。PostgreSQL 也是类似的思路，但实现方式不同，用 tuple 的 xmin/xmax 字段来标记数据版本。"

---

### Q10：分布式锁是怎么实现的？

**面试话术**：

"我的项目用 Redis 分布式锁，核心是 SETNX 加过期时间。

加锁的时候，用 Redis 的 SET NX EX 命令，原子性地设置一个带过期时间的 key。能设置成功就说明拿到了锁，同时我会生成一个唯一的锁 ID 存进去，防止误删别人的锁。

释放锁的时候，要先验证这个锁是不是自己加的，也就是对比锁 ID。验证通过了才删除这个 key。但是判断 + 删除这两步要保证原子性，不然可能判断时是自己的锁，删除时锁已经过期被别人拿走了。所以释放锁用 Lua 脚本来做，把判断和删除打包成一个原子操作。

我的项目里，商品详情缓存重建时用了分布式锁防止击穿。缓存失效之后，所有请求都想去数据库重建，我只让拿到锁的第一个请求去查，其他的等一下再读缓存。

为什么要给锁设过期时间？防止持锁的服务挂了锁没释放，导致死锁，别人永远拿不到锁。过期时间设多长要根据业务来，比我的业务逻辑执行时间稍长一点就行。"

**追问：如果 Redis 挂了怎么办？**
> "这是单点故障。生产环境建议用 Redis Cluster 或者 Redlock 算法。Redlock 的思路是在多个独立的 Redis 节点上同时加锁，超过半数节点加锁成功才算真正拿到锁。这样即使一个节点挂了，锁机制还能正常工作。"

---

### Q11：Linux CPU 100% 怎么排查？

**面试话术**：

"CPU 100% 排查分四步。

第一步，用 top 命令找到占用 CPU 最高的进程，按 P 键可以按 CPU 排序，看哪个进程在飙高。或者用 ps aux 加 sort 参数找出前几名。

第二步，如果是 Python 进程，用 top -H -p 加进程号，可以看到线程级别的 CPU 占用，找到是哪个线程在跑满。

第三步，用 py-spy 这个工具，可以实时看 Python 进程的调用栈，不用停进程就能知道代码在哪里卡着。这个比看日志直观多了。

第四步，根据调用栈定位到具体的代码，看是死循环、慢查询还是正则回溯的问题。

常见的原因有：死循环、正则写得太复杂导致回溯爆炸、数据库查询加了锁一直在等待、GIL 争用导致大量上下文切换。

我的项目里 Celery worker 有次 CPU 飙高，排查发现是邮件任务堆积太多，SMTP 服务器响应慢，任务一直在等。给邮件任务加了限流之后就恢复正常了。"

**追问：如何优化死循环？**
> "先用 py-spy 或者调用栈定位到具体是哪个循环，看循环终止条件是不是写错了。如果是业务必要的循环，考虑加 sleep 或者改成异步处理，不要让它占满 CPU。"

---

### Q12：数据库连接池满了怎么排查？

**面试话术**：

"连接池满了排查分三步。

第一步，查当前连接数。PostgreSQL 里执行查询，看 pg_stat_activity 视图，可以看到所有活跃的连接。同时查一下 max_connections，看配置的上限是多少，对比一下当前用了多少。

第二步，看哪些连接在做什么。还是在 pg_stat_activity 里，看每个连接的 state 和 query，找到那些长时间 active 或者 idle in transaction 的连接，这些是占着茅坑不拉屎的。

第三步，找原因。常见的原因有几个：代码里数据库连接用完没关，比如游标或连接没有用 with 语句自动关；有长事务一直没提交，把连接占住了；慢查询执行时间太长，连接被一直占用；或者是连接池大小配太小，并发高了不够用。

解决方案就是对症下药。代码问题就改代码，用 with 语句管理连接。慢查询就加索引或者优化。连接池不够就扩大配置。另外给连接数设置监控报警，快满的时候提前知道，比出问题了再排查要好。

我的项目 settings 里配了 CONN_MAX_AGE，让连接复用，不是每次请求都重新建连接，能有效减少连接数。"

**追问：CONN_MAX_AGE 设置多大合适？**
> "看业务场景。频繁访问数据库的接口，设长一点比如 600 秒，减少反复建连接的开销。如果是 WebSocket 这种长连接服务，设短一点，避免连接老化失效。一般 Web 接口我会设 300 到 600 秒。"

---

### Q13：N+1 问题怎么排查和解决？

**面试话术**：

"N+1 问题就是查了一次主表，又查了 N 次关联表，效率很低。比如我要查 20 个商品，本来应该一次 SQL 就查出来，但如果代码写得不好，可能会先查一次商品列表，然后再去查每个商品的分类，总共 21 次 SQL。

怎么排查？可以用 Django Debug Toolbar，它能在页面上显示每次请求执行了多少条 SQL，每条 SQL 花了多长时间。

怎么解决？Django ORM 提供了两个方法：select_related 和 prefetch_related。select_related 用的是 JOIN 查询，适合一对一、一对多关系，一次 SQL 就能拿到主表和关联表的数据。prefetch_related 用的是分两次查询，先查主表，再查关联表，在 Python 内存里组合起来，适合多对多关系。

我的项目里商品列表页一开始有 N+1 问题，加了 select_related('category') 之后，SQL 从 21 次降到了 1 次，性能提升很明显。

什么时候用哪个？如果是一对一或者外键关联，用 select_related；如果是多对多，用 prefetch_related。"

---

### Q14：Celery 任务失败了怎么排查？

**面试话术**：

"Celery 任务失败排查，我做了三层。

第一层是统一捕获失败。我用了 Celery 的信号机制，任何任务只要失败了，都会触发一个钩子函数。这个钩子函数会把任务名称、任务ID、异常信息、堆栈跟踪都存到数据库里。这样我随时可以查历史失败记录，不用去看日志文件。

第二层是 Celery 自带的命令。可以用 inspect 命令查看当前有哪些任务正在执行、哪些任务失败了、每个 worker 的状态等等。

第三层是日志分析。Celery 的 worker 进程会把日志输出到文件，如果任务失败了，错误信息和堆栈都会写在日志里。

常见的失败原因有几种：网络问题，比如连不上邮件服务器；参数错误，比如邮箱地址是空的；超时，任务执行时间太长超过了限制；还有 worker 进程自己崩溃了。

怎么防止任务丢失？我配置了一个参数，让任务执行成功之后才从队列里删除，如果 worker 执行到一半挂了，任务会重新入队，不会丢。另外也配置了结果后端，可以用任务ID去查任务当前是什么状态。"

---

### Q15：Redis 内存满了怎么办？

**面试话术**：

"Redis 内存满了之后，新数据写不进去，需要配置一个回收策略。

Redis 有好几种回收策略，我一般用的是 allkeys-lru，就是删除最近最少使用的 key。这样可以保留热点数据，淘汰不常用的数据。

其他的策略还有：noeviction 就是不删除，满了之后写操作直接报错；volatile-lru 只删除设置了过期时间的 key；allkeys-random 随机删除；volatile-ttl 删除最快要过期的 key。

我的项目配置了内存上限和回收策略，当内存快满的时候，Redis 会自动删除一些不常用的 key。

预防措施有几个：给每个 key 设置合理的过期时间，不要让它无限期存在；不要在 Redis 里存大的数据，比如图片文件；定期清理无用的 key；监控内存使用率，设置报警阈值，快满的时候提前处理。

如果要找占用内存最多的 key，可以用 Redis 的 bigkeys 命令，或者用专门的工具分析 RDB 文件。"

**追问：如何找到占用内存最多的 key？**
> "用 redis-cli --bigkeys 或 RDB 工具分析。如果确定某些 key 不用了，可以用 UNLINK（异步删除）或 DEL 命令删除。"

---

### Q16：ORM 查询优化有什么技巧？

**面试话术**：

"ORM 查询优化我总结了几个常用技巧。

第一是只查需要的字段。默认查询会查出所有字段，但很多时候我们只需要几个字段，这时候用 values 或者 values_list 只查需要的字段，减少数据传输量和内存占用。

第二是分页。数据量大的时候，不要一次性查所有数据，分页查询。但是要注意，传统的 OFFSET 分页在数据量大的时候性能不好，因为数据库还是要把前面的数据都扫一遍。可以用游标分页，用上一页最后一条数据的 ID 来查下一页。

第三是索引优化。给经常查询的字段加索引，比如商品名称、创建时间。加索引之后查询速度能快几十倍甚至上百倍。

第四是聚合查询用 annotate，在数据库层面做聚合，而不是在 Python 里循环计算。

第五是避免在循环里查数据库，这就是 N+1 问题，前面已经说过了。

第六是用 explain 查看执行计划，看看数据库是怎么执行这个查询的，有没有用到索引，是不是全表扫描。

我的项目商品列表页优化之后，查询时间从 500 毫秒降到了 50 毫秒。

什么时候用原生 SQL？当 ORM 搞不定的复杂查询或者性能要求特别高的时候，可以写原生 SQL。但是尽量先用 ORM，原生 SQL 可维护性差。"

**追问：什么时候用 raw SQL？**
> "ORM 搞不定的复杂查询或性能要求高的场景，比如涉及多个表的复杂 JOIN、窗口函数、存储过程调用。但尽量先用 ORM，raw SQL 最后考虑，因为可维护性差。"

---

### Q17：接口突然变慢了怎么排查？

**面试话术**：

"接口变慢的排查，我按这个思路来。

第一步是定位是哪个接口慢。可以用 Django Debug Toolbar，它在页面上显示每个请求的总耗时和各部分耗时，比如数据库查询花了多少、渲染花了多少。或者用 APM 工具像 New Relic、DataDog，能看到所有接口的响应时间和历史趋势。

第二步是看数据库查询。Django Debug Toolbar 会显示每条 SQL 的执行时间，找到那些特别慢的 SQL，可能是没有索引、数据量太大或者查询写得不好。

第三步是看缓存命中率。用 Redis 的 info 命令可以看缓存的命中情况，如果命中率很低，说明很多请求都穿透缓存打到数据库了。

第四步是看系统资源。用 top 看是不是 CPU 飙高，用 free 看内存是不是快满了，用 iostat 看磁盘是不是卡了。

第五步是看日志。用 tail -f 实时查看日志，看看有没有错误或者异常信息。

常见的原因有：数据库慢查询，比如没加索引或者数据量太大；缓存失效了，所有请求都打到数据库；调用第三方 API 超时了；代码逻辑有问题，比如在循环里查数据库。

我的项目里支付回调接口变慢过，排查发现是支付宝验签的时候网络抖动，加了重试和超时控制之后就正常了。"

**追问：如何监控接口性能？**
> "可以用 APM 工具（New Relic、DataDog），或自己埋点记录每个接口的响应时间。设置报警阈值，比如 P95 超过 1 秒就报警。"

---

## 高频追问 Q18-Q25（概念原理层）

---

### Q18：什么是死锁？什么是分布式锁？

**面试话术**：

"死锁就是两个或多个进程互相等待对方释放资源，导致无限阻塞。举个经典的例子：进程 A 拿着锁 1，在等锁 2；进程 B 拿着锁 2，在等锁 1。两个人都等着对方先释放，结果谁也动不了，永远等下去。

数据库里也会有死锁。比如事务 A 更新了商品 1 的库存，想再更新商品 2；事务 B 更新了商品 2 的库存，想再更新商品 1。两个事务都等着对方释放锁，就死锁了。

怎么解决？数据库会自动检测到死锁，然后回滚其中一个事务。应用层也可以设置锁的超时时间，等太久就放弃。

分布式锁是解决跨多个服务实例的并发问题的。比如你有 3 个 Web 服务器，都连同一个 Redis，要确保同一时刻只有一个服务器能操作某个资源。这就是分布式锁。

实现分布式锁的核心是要满足三个条件：互斥性，同一时刻只有一个客户端持锁；防死锁，即使持锁的服务器崩溃，锁也要能自动释放；避免误删，不能释放别人的锁。

用 Redis 实现的话，就是设置一个带过期时间的 key，只有能设置成功的才算拿到锁。释放锁的时候要验证是不是自己持有的锁，防止删错了。为了保证原子性，释放锁最好用 Lua 脚本或者 Redlock 算法。

我的项目里，商品详情缓存重建的时候就用了分布式锁防止击穿。缓存失效之后，所有请求都想去查数据库重建缓存，但我只允许拿到锁的那个请求去查，其他的先等一下，等缓存建好了再读。"

---

### Q19：进程池和线程池是什么？项目用不用？好处和坏处？

**面试话术**：

"进程池就是预先创建一组进程，这些进程一直在那，任务来了就从队列里取一个处理，处理完了不销毁，继续处理下一个任务。线程池类似，只是用线程而不是进程。

进程池和线程池的区别有几个。进程之间内存是隔离的，每个进程有独立的内存空间，适合 CPU 密集型任务，因为 Python 的 GIL 问题不影响。线程之间共享进程内存，受 GIL 限制，同一时刻只有一个线程在执行 Python 代码，但线程的创建和切换开销小，适合 I/O 密集型任务，比如网络请求、文件读写。

我的项目里用到了进程池和线程池。Gunicorn 用的是进程池，配置了几个 worker 进程来处理并发请求。Celery 默认也是用进程池模式。如果要做 I/O 密集型的任务，可以用 gevent 或者 eventlet，这是线程池的实现，我的项目里安装了 eventlet。

好处很明显。第一是减少了创建和销毁的开销，进程和线程复用，不用每次都新建。第二是能控制并发数量，避免资源被耗尽。第三是提高了吞吐量，任务队列异步处理，请求响应更快。

坏处也有。进程池的内存占用大，因为每个进程都有独立的内存空间。进程间通信比较复杂，需要用队列或者共享内存。任务队列满的时候可能阻塞，需要设置合理的大小。另外，进程崩溃可能导致任务丢失，需要监控和重试机制。

这里顺便说一下 Python 的 GIL 问题。GIL 是全局解释器锁，确保同一时刻只有一个线程执行 Python 字节码。这意味着多线程在 CPU 密集型任务上无法并行，只能并发。但是 I/O 密集型任务不受影响，因为等待 I/O 的时候会释放 GIL。所以 CPU 密集型任务用多进程，I/O 密集型用多线程或者 asyncio。"

---

### Q20：Redis 的缓存机制是怎么运行的？

**面试话术**：

"Redis 的缓存读写流程通常是 Cache-Aside 模式。读的时候，先查 Redis，如果命中就直接返回；如果没命中，就去查数据库，然后把结果写到 Redis 里，再返回。写的时候，推荐先更新数据库，再删除缓存，而不是更新缓存。因为更新缓存和更新数据库这两个操作不是原子的，可能会出现数据不一致。

Redis 内部用了不同的数据结构来存储数据。String 用的是 SDS（简单动态字符串），List 用的是 quicklist（双向链表加压缩列表），Hash 在数据少的时候用 ziplist，数据多了切换成哈希表，Set 用 intset 或者哈希表，有序集合用 ziplist 或者跳表加哈希表。

我的项目里商品详情的缓存就是这样实现的。先查 Redis，有就直接返回；没有就查数据库，然后写缓存，过期时间设置一个随机偏移，防止雪崩。商品更新之后，我不更新缓存，而是直接删除缓存，让下一次请求重新从数据库读，保证一致性。"

---

### Q21：秒杀功能怎么实现？如何防止超卖？出现超卖怎么解决？

**面试话术**：

"秒杀的核心挑战有三个：超高并发导致数据库扛不住、超卖导致库存变负数、热点商品导致缓存击穿。

如果让我设计秒杀，我会用 Redis 预减库存加消息队列的方案。

第一步，在 Redis 里预先准备好库存。用户来抢的时候，直接在 Redis 里扣库存。为了保证原子性，用 Lua 脚本检查库存是否足够，够了就扣减。这样即使一万个人同时来抢，Redis 也能扛住，不会打穿数据库。

第二步，生成一个秒杀令牌，防止一个用户重复抢购。如果发现这个用户已经抢过了，就把刚才扣掉的库存加回去。

第三步，Redis 扣库存成功之后，把创建订单的任务发到消息队列里异步处理。前端马上就能返回秒杀成功，用户体验好，后端慢慢创建订单和扣减数据库库存。

第四步，数据库层面用 F 表达式做原子扣减，兜底防止超卖。

如果真的超卖了怎么处理？先立即停止秒杀，然后对比 Redis 剩余库存和数据库实际库存的差异，优先退款给超卖的用户，补货后再重启。

流量不大的场景，也可以直接用数据库的 SELECT FOR UPDATE 悲观锁，先锁这行库存记录再扣减，简单直接，但并发上不去。"

---

### Q22：悲观锁和乐观锁是什么？区别和作用？

**面试话术**：

"悲观锁和乐观锁是两种处理并发冲突的策略。

悲观锁的思路是，我去操作这条数据之前先把它锁住，不让别人碰。数据库里用 SELECT FOR UPDATE，事务里一锁定，其他事务要等我提交或者回滚才能操作这条数据。好处是安全，不会有冲突。坏处是锁的粒度大，并发高的时候互相等待，性能差。

乐观锁的思路是，我先读出数据，不加锁，去做计算。更新的时候再检查一下版本号，如果版本号还是我读的时候那个，说明没人改过，我就直接更新并把版本号加一。如果版本号变了，说明有人在我读完之后改了，这时候我重试一下。乐观锁适合读多写少的场景，并发性能好。

Django 的 F 表达式是乐观锁的一种实现。它直接在数据库层面执行库存减操作，不是先读出来再写回去，所以多个请求同时来，数据库会串行执行这些更新，不会出现两个人读到同样的值各自扣减的问题。

我的项目库存扣减用 F 表达式，因为是读多写少的电商场景，乐观锁性能更好。如果真的有超卖风险，可以改成 SELECT FOR UPDATE 悲观锁，代价是并发性能下降。"

---

### Q23：IP 验证这块有什么坑？

**面试话术**：

"IP 验证有几个常见的坑。

第一个是 X-Forwarded-For 伪造的问题。客户端可以在请求头里写多个假 IP。取 IP 的时候要注意，要取离服务器最近的那跳，也就是最右边的 IP，不能直接取最左边的，因为最左边是客户端自己写的，可以伪造。

第二个是用了 CDN 或者 WAF 之后，REMOTE_ADDR 其实是 CDN 节点的 IP，不是用户真实 IP。这时候要从 CDN 传递的特定请求头里拿，比如 Cloudflare 的 Cf-Connecting-IP，或者 Nginx 配置的 X-Real-IP。

第三个是 IPv6 支持。如果数据库字段只考虑了 IPv4 的格式，存不了 IPv6 地址。用 Django 的 GenericIPAddressField 就能同时支持两种格式。

第四个是白名单 JSON 解析错误。用户的 IP 白名单存的是 JSON，如果数据损坏了或者格式不对，解析会报错。要加异常处理，解析失败就直接返回不可信，不能让异常传出去。

第五个是 IP 频繁变动的问题。用户从 WiFi 切到 4G，IP 肯定变。强制登出体验不好。更友好的做法是结合设备指纹，比如 User-Agent 加 Cookie，允许 IP 在一定范围内变动，不要只靠 IP 做身份识别。"

---

### Q24：Linux 常用命令及其用处？



**面试话术**：

"Linux 常用命令分几类来说。

系统监控类的命令：top 实时查看进程 CPU 和内存占用，按 P 键可以按 CPU 排序，按 M 键按内存排序；htop 是 top 的增强版，界面更友好；ps aux 可以查看所有进程；free -h 看内存使用情况，带单位易读；df -h 看磁盘使用情况；iostat 看磁盘 IO。

文件操作类的命令：ls -la 列出文件，包括隐藏文件和权限；cd 切换目录；pwd 显示当前目录；mkdir 创建目录，加 p 参数可以递归创建；rm 删除文件或目录，加 r 参数递归，f 强制删除，很危险要小心；cp 复制文件或目录，r 参数递归；mv 移动或重命名。

文本处理类：cat 查看文件内容；less 分页查看，可以上下翻；head 看前几行；tail -f 实时查看日志文件；grep 搜索文件内容，r 参数递归搜索；awk 提取特定列；sort 排序；uniq 去重；wc 统计行数。

权限管理：chmod 修改文件权限，755 是常用权限；chown 修改文件所有者；ls -l 查看文件权限和所有者。

网络调试：ping 测试网络连通性；curl 查看 HTTP 响应；netstat 或者 ss 查看监听端口；telnet 测试端口连通性。

进程管理：kill 杀进程，9 是强制杀；pkill 按名字杀进程；systemctl 管理系统服务，启动、停止、查看状态、看日志。

压缩解压：tar 打包压缩，x 解压，z 是 gzip，c 是创建；zip 和 unzip 是 zip 格式。

Django/Celery 相关：python manage.py runserver 启动开发服务器；celery -A project worker 启动 worker；inspect active 查看活跃任务；purge 清空任务队列。"

---

### Q25：Docker 常用命令及其用处？

**面试话术**：

"Docker 命令分镜像、容器、日志几类。

镜像操作类：docker images 查看本地镜像；docker pull 拉取远程镜像；docker build 根据当前目录的 Dockerfile 构建镜像，t 参数指定名字；docker rmi 删除镜像；docker tag 给镜像打标签。

容器操作类：docker ps 查看运行中的容器，a 参数显示所有容器包括停止的；docker run 运行容器，d 参数后台运行，p 参数映射端口，name 参数指定名字；docker start、stop、restart 分别启动、停止、重启容器；docker rm 删除容器；docker exec 进入容器内部，it 参数交互式终端。

日志和调试类：docker logs 查看容器日志，f 参数实时刷新；docker inspect 查看容器详细信息；docker stats 查看容器资源使用情况，CPU、内存、网络。

Docker Compose 相关：docker-compose up -d 启动所有服务，d 是后台运行；down 停止并删除所有容器；ps 查看服务状态；logs 查看特定服务日志；exec 进入某个服务容器；build 重新构建镜像。

我的项目用 Docker Compose 管理 Redis，配置了镜像版本、端口映射、数据卷挂载、启动命令。常用命令就是 up -d 启动、down 停止、exec 进入 redis-cli。

Docker 的优势有四个：一是环境一致性，开发、测试、生产环境完全一样；二是快速部署，镜像复用；三是资源隔离，每个容器独立环境；四是便于扩展，快速启动多个实例扩容。"

---

## 通用基础题 Q26-Q35（补充层）

---

### Q26：GIL 是什么？对多线程有什么影响？

**面试话术**：

"GIL 是全局解释器锁，是 Python 解释器的一个机制，它的作用是确保同一时刻只有一个线程在执行 Python 字节码。

GIL 对多线程程序的影响主要体现在 CPU 密集型任务上。因为 GIL 的存在，即使你有多个 CPU 核心，多线程也无法真正并行执行，只能并发。比如你要做一个密集的计算任务，用多线程其实不会变快，甚至可能因为线程切换变得更慢。

但是 I/O 密集型任务不受影响，因为 Python 在等待 I/O 的时候会释放 GIL，其他线程可以趁机执行。比如网络请求、文件读写、数据库查询，这些操作用多线程是有效的，能提高并发性能。

怎么解决 GIL 的限制？CPU 密集型任务可以用多进程，因为每个进程有独立的 GIL，可以真正利用多核。或者用 asyncio 异步编程，这个是单线程并发，不需要 GIL 锁。"

---

### Q27：装饰器是什么？有什么作用？

**面试话术**：

"装饰器本质上是一个函数，它接收一个函数作为参数，返回一个新的函数。它的作用是在不修改原函数代码的情况下，给函数增加额外的功能。

打个比方，装饰器就像给手机加一个保护壳。手机还是原来的手机，但是有了保护壳，就有了防摔、美观这些新功能。

装饰器的语法是用 @ 符号放在函数定义的前面。比如我要记录一个函数的执行时间，可以写一个计时装饰器，然后在需要计时的函数上面加上 @timer 这个装饰器。

常用的场景有：日志记录、权限检查、性能监控、缓存结果、重试机制。比如我项目里的 API 接口，加了一个登录验证装饰器，如果用户没登录就直接返回 403，不需要在每个接口里都写一遍登录验证的代码。

装饰器的好处是代码复用，把通用的功能抽出来，需要用的时候加个装饰器就行，代码更干净。"

---

### Q28：深拷贝 vs 浅拷贝

**面试话术**：

"深拷贝和浅拷贝的主要区别是对嵌套对象的处理方式。

浅拷贝只复制对象本身，不复制里面的嵌套对象。比如一个列表里包含了另一个列表，浅拷贝之后，外层列表是新的，但里面的那个子列表还是原来的引用。修改外层不会影响原对象，但修改里面的子列表会同时影响原对象。

深拷贝是完全复制，包括所有嵌套对象。复制之后，新对象和原对象完全独立，互不影响。无论修改哪一层，都不会影响对方。

Python 里用 copy 模块来实现。copy.copy 是浅拷贝，copy.deepcopy 是深拷贝。另外列表的切片、dict.copy、list.copy 这些都是浅拷贝。

什么时候用哪种？如果对象比较简单，没有嵌套结构，或者你知道只是复制外层就够了，用浅拷贝，性能更好。如果对象很复杂，有深层的嵌套，而且你希望复制之后完全独立，就用深拷贝。"

**追问：为什么有时候用浅拷贝就够了？**
> "比如配置对象，你只想修改某个属性的值，不想影响原对象，浅拷贝就够用了。而且深拷贝有性能开销，大对象深拷贝会慢。"

---

### Q29：`__new__` vs `__init__` 的区别

**面试话术**：

"`__new__` 和 `__init__` 都是创建对象时用到的方法，但职责不同。

`__new__` 负责创建对象，是在内存里分配空间，返回这个新对象的实例。它是一个类方法，在对象被创建之前调用，先于 `__init__` 执行。

`__init__` 负责初始化对象，是在对象已经创建好之后，给它设置初始属性。它是一个实例方法，在 `__new__` 返回对象之后调用。

打个比方，`__new__` 像是盖房子，先打地基、建框架；`__init__` 像是装修，房子建好了之后，再放家具、刷墙。

正常情况下，我们很少需要重写 `__new__`，因为默认的实现已经够用了。但有些特殊场景会用到，比如单例模式，在 `__new__` 里判断对象是否已存在，存在就直接返回，不存在才创建。或者不可变类型，比如 int、str、tuple 的创建逻辑就写在 `__new__` 里。

大部分时候我们只需要写 `__init__`，在这里设置对象的初始状态。"

---

### Q30：TCP 三次握手/四次挥手

**面试话术**：

"三次握手是建立 TCP 连接的过程，四次挥手是断开连接的过程。

三次握手的步骤：第一次，客户端发一个 SYN 包给服务器，告诉对方我想建立连接。第二次，服务器收到 SYN，回一个 SYN-ACK 包，意思是收到了，我也想建立连接。第三次，客户端收到 SYN-ACK，回一个 ACK 包，确认连接建立。

为什么需要三次？主要是确认双方的收发能力都正常。第一次握手确认服务器能收；第二次握手确认客户端能收能发；第三次握手确认服务器能发。如果只握手两次，服务器不知道客户端是不是真的能收到它的确认。

四次挥手的步骤：第一次，客户端发一个 FIN 包，说我要断开了。第二次，服务器收到 FIN，回一个 ACK，表示收到了，但可能还有数据要传，先不断开。第三次，服务器数据传完了，发 FIN 包，说自己也准备好了。第四次，客户端收到 FIN，回 ACK，连接彻底断开。

为什么挥手需要四次？因为 TCP 是全双工的，两个方向可以独立关闭。客户端说我不发了，但服务器可能还有数据要发给客户端，所以要等服务器也准备好断开才行。"

---

### Q31：HTTP vs HTTPS 的区别

**面试话术**：

"HTTPS 是 HTTP 的安全版本，主要区别有三个。

第一是安全性。HTTP 是明文传输，数据没加密，容易被窃听和篡改。HTTPS 用 SSL/TLS 协议加密传输，第三方截获了也看不懂内容，更安全。

第二是端口不同。HTTP 默认用 80 端口，HTTPS 用 443 端口。

第三是需要证书。HTTPS 需要申请 SSL 证书，有免费的也有付费的。证书用来验证服务器的身份，防止中间人攻击。

HTTPS 的缺点是性能会稍微差一点，因为加密解密需要额外的计算。但现在的服务器性能都够用，影响不大。所以现在的网站几乎都用 HTTPS 了。

工作原理是这样的：浏览器访问 HTTPS 网站，服务器把公钥证书发给浏览器。浏览器验证证书是不是合法的，如果是，就用公钥加密一个随机密钥发给服务器。服务器用自己的私钥解密得到这个密钥，之后双方用这个密钥对称加密通信。"

---

### Q32：HTTP 常见状态码

**面试话术**：

"HTTP 状态码分几类，我记最常用的几个。

2xx 成功类：200 是请求成功，返回数据。201 是资源创建成功，比如提交订单成功。204 是成功但没有内容返回。

3xx 重定向类：301 是永久重定向，比如网站换了域名，以后都跳新域名。302 是临时重定向，比如活动页跳转到详情页。304 是缓存未修改，浏览器可以用缓存的资源。

4xx 客户端错误类：400 是请求参数有问题。401 是未认证，没登录或者 token 过期。403 是禁止访问，登录了但没权限。404 是资源不存在。405 是请求方法不对，比如用 GET 访问只支持 POST 的接口。429 是请求太频繁，被限流了。

5xx 服务器错误类：500 是服务器内部错误，代码有 bug。502 是网关错误，比如 Nginx 后端的服务挂了。503 是服务不可用，比如服务器维护中。504 是网关超时，后端处理太慢了。

我在项目里最常遇到的是 401、403、404、500，这些都有对应的错误处理页面，给用户友好的提示。"

---

### Q33：跨域问题怎么解决？

**面试话术**：

"跨域是浏览器的安全策略，不允许前端页面向不同域名的接口发请求。比如前端在 a.com，接口在 b.com，默认会跨域报错。

判断跨域看三点：协议、域名、端口，只要有一个不一样就算跨域。a.com 和 b.com 是跨域，http 和 https 是跨域，80 端口和 8080 端口也是跨域。

解决跨域有几种方法。

第一种是在后端设置响应头，Access-Control-Allow-Origin，允许指定的域名跨域访问。如果是生产环境，可以写死允许的域名列表。如果是开发环境，可以写成通配符 * 允许所有域名。

第二种是用 Nginx 反向代理，让前端和接口看起来是同一个域名。比如前端访问 a.com/api/xxx，Nginx 把请求转发到 b.com/xxx，浏览器看来是同域请求。

第三种是 JSONP，这个是老方案，只能用 GET 请求，现在基本不用了。

我的项目里用的是第一种，在 Django 的中间件里设置了 CORS 响应头，允许前端域名跨域访问。"

---

### Q34：索引是什么？为什么能加速查询？

**面试话术**：

"索引就像书的目录。没有目录的话，找一页书要从头翻到尾，很慢。有了目录，可以直接定位到那一页。

数据库索引的本质是一种数据结构，常用的是 B+树。B+树的特点是层数少、查询快，即使有千万级数据，也只需要查几次就能找到。

为什么能加速查询？因为索引把数据按顺序排好了，查询的时候可以用二分查找，时间复杂度是 O(log n)。如果没有索引，数据库要全表扫描，时间复杂度是 O(n)，数据量大就慢很多。

索引的缺点也有几个。第一是占用空间，索引是额外的数据结构，要存额外的数据。第二是增删改的时候要更新索引，会变慢。第三是索引太多，数据库选索引的时候要花时间判断用哪个。

什么时候建索引？经常作为查询条件的字段，比如用户 ID、订单号、商品名称。什么时候不建索引？数据量小的表，或者很少查询的字段，或者增删改特别频繁的字段。

我在项目里给订单表的用户 ID、商品 ID、创建时间都建了索引，查询速度提升很明显。"

---

### Q35：事务的四个特性（ACID）

**面试话术**：

"事务的四个特性叫 ACID，分别代表原子性、一致性、隔离性、持久性。

原子性就是一个事务里的操作要么全成功，要么全失败。比如转账，A 转给 B 100 块，A 的账户扣了 100，B 的账户加 100，这两个操作要么都成功，要么都失败，不能出现 A 扣了但 B 没加到的情况。数据库通过 undo log 实现原子性，失败的时候回滚。

一致性就是事务执行前后，数据库的状态要保持一致。比如转账前后，A 和 B 的总金额不变。这个是业务层面的约束，数据库只能保证操作的原子性，一致性的约束需要应用层自己定义。

隔离性就是多个事务并发执行的时候，互不干扰。比如 A 和 B 同时查同一个数据，如果 A 修改了但还没提交，B 读到的应该是修改前的数据。数据库通过锁机制和 MVCC 实现隔离性。

持久性就是事务提交成功之后，数据就永久保存了，即使数据库崩溃也不会丢失。数据库通过 redo log 实现持久性，提交的时候先把日志写到磁盘，确保不丢。

事务隔离级别有四个：读未提交、读已提交、可重复读、串行化。级别越高，隔离性越好，但并发性能越差。PostgreSQL 默认是可重复读，MySQL 默认是读已提交。"

**追问：你的订单支付怎么保证事务？**
> "支付回调接口开了事务，先查订单状态，如果是待支付就改成已支付，同时扣减库存。如果任何一个步骤失败，事务回滚，保证状态一致。"

---

## 项目部署 & Docker 化

### 部署架构
```
Nginx (反向代理 + 静态文件)
   ↓
Gunicorn (4 workers, 进程池)
   ↓
Django (业务逻辑)
   ↓
PostgreSQL (数据存储) + Redis (缓存/会话/队列)
```

### Docker 化步骤
```dockerfile
# Dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["gunicorn", "project.wsgi:application", "-w", "4", "-b", "0.0.0.0:8000"]
```

```yaml
# docker-compose.yml
version: '3'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis

  db:
    image: postgres:15
    environment:
      POSTGRES_DB: shop_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    ports:
      - "6379:6379"

volumes:
  db_data:
```

---

## 面试自信话术

### 开场白
"您好，我叫XXX，是一名后端开发工程师，主要技术栈是 Django + PostgreSQL + Redis。我做的最拿得出手的项目是 BaykeShop 电商系统，我基于开源框架做了深度二次开发，实现了商品管理、订单支付、缓存优化、IP 白名单安全等功能。项目上线后稳定运行，日活用户XXX，订单量XXX。"

### 回答问题的技巧
1. **先说结论，再展开**
   "这个问题我考虑过，我的方案是..."

2. **结合项目讲**
   "在我的项目里，我遇到了类似的问题，当时我用了..."

3. **不怕说不知道**
   "这个问题我了解得不够深，但我的思路是..."

4. **展示思考过程**
   "如果让我设计，我会从这几个方面考虑：..."

### 面试官追问时的应对
"这个问题问得很好，我没想过，但我回去后会深入研究一下。我的理解是..."

### 结束语
"这个项目让我学到了很多，特别是高并发场景下的缓存设计和数据库优化。我期待能加入贵公司，把我的经验应用到实际项目中。"

---

**祝面试顺利！记住，自信 + 真实 + 有思考 = 好印象。**

---

## 补充面试题 Q36-Q55（高频八股防守层）

---

### Q36：`*args` 和 `**kwargs` 是什么？

**面试话术**：

"`*args` 接收多余的位置参数，打包成元组。`**kwargs` 接收多余的关键字参数，打包成字典。配合使用可以让函数接收任意数量的参数。装饰器里经常用到，因为不知道被装饰的函数有哪些参数，用 `*args, **kwargs` 全部接住再传过去。调用时也可以解包，`*列表` 拆成位置参数，`**字典` 拆成关键字参数。

---

### Q37：生成器和迭代器有什么区别？

**面试话术**：

"迭代器是实现了 `__iter__` 和 `__next__` 的对象，可以用 for 循环遍历。生成器是迭代器的一种，用函数加 `yield` 实现，调用时不立即执行，每次 next 暂停在 yield 处，按需生成数据，省内存。迭代器是协议，生成器是实现。列表不是迭代器，但可以用 iter() 转成迭代器。

---

### Q38：闭包是什么？有什么坑？

**面试话术**：

"闭包是内部函数记住了外部函数的变量，即使外部函数执行完了还能访问。实现方式：外部函数定义内部函数，内部函数引用外部变量，外部函数返回内部函数。

坑：延迟绑定。比如循环里创建闭包引用循环变量，结果所有闭包都绑定了循环结束后的值。解决方法是把循环变量作为参数传入，或用默认参数固定当时值。我的项目里装饰器就是闭包。

---

### Q39：`classmethod`、`staticmethod` 和实例方法有什么区别？

**面试话术**：

"实例方法第一个参数是 self，必须通过实例调用，访问实例和类属性。类方法用 @classmethod，第一个参数是 cls，可通过类或实例调用，用来操作类属性或工厂方法创建实例。静态方法用 @staticmethod，没有默认参数，跟普通函数一样只是挂在类下面，适合工具函数。按需选择即可。

---

### Q40：Django 的 MVT 是什么？请求生命周期是怎样的？

**面试话术**：

"MVT 是 Model-View-Template。Model 管数据，View 管业务逻辑，Template 管页面渲染。跟 MVC 类似，只是 Django 的 View 对应 MVC 的 Controller，URL 路由分发就是 Controller 的作用。

请求流程：浏览器请求 → WSGI 服务器 → 中间件 process_request → URL 路由匹配 View → View 处理业务（可能查 Model） → 返回响应 → 中间件 process_response → 浏览器。中间件是全局钩子，可以做认证、日志、跨域等横切处理。

---

### Q41：Django 中间件是什么？怎么写一个自定义中间件？

**面试话术**：

"中间件是请求响应的全局钩子，做认证、日志、跨域、限流等横切逻辑。核心方法有：process_request 路由前执行，process_view View 执行前，process_exception 异常时，process_response 返回前。request 顺序执行，response 逆序执行。

自定义方式：继承 MiddlewareMixin 实现钩子方法，或写类定义 `__init__` 和 `__call__`，`__call__` 里用 try-finally 包裹 get_response。写完在 settings 的 MIDDLEWARE 列表注册，顺序很重要。

---

### Q42：DRF 的认证和权限机制是怎样的？

**面试话术**：

"DRF 的认证和权限分开，执行顺序是认证→权限。认证确认用户是谁：SessionAuthentication 适合浏览器，TokenAuthentication 适合移动端，JWT 需额外装包。配置多个认证类按顺序尝试。权限确认能做什么：IsAuthenticated 要求登录，IsAdminUser 要求管理员，IsAuthenticatedOrReadOnly 只读允许游客。可以继承 BasePermission 自定义，在视图里用 permission_classes 指定。

---

### Q43：Django Signal 是什么？适用场景有哪些？

**面试话术**：

"Signal 是 Django 的信号机制，观察者模式。某个动作发生时发送信号，注册的处理函数自动调用，实现解耦。常用内置信号：post_save 模型保存后，pre_delete 删除前。适用场景：用户注册后发欢迎邮件，订单创建后更新库存，模型修改后清缓存。但信号是同步执行，处理逻辑太重会阻塞主流程，而且不好调试，所以核心业务逻辑还是直接调用更可控。

---

### Q42：`select_related` 和 `prefetch_related` 的区别和使用场景？

**面试话术**：

"`select_related` 用 SQL JOIN 连表查询，适用于外键和一对一正向关联，一次查出所有数据。`prefetch_related` 分开查，先查主表再用 IN 查关联表，在 Python 层面匹配，适用于多对多和反向关联。选哪个：一对一和外键用 select_related，多对多用 prefetch_related。注意 prefetch_related 之后不要再过滤关联表，否则优化失效。

---

### Q45：元组和列表有什么区别？什么时候用元组？

**面试话术**：

"元组不可变，列表可变。元组用圆括号，可以作为字典 key，占用内存少，创建遍历稍快。列表用方括号，有 append、pop 等增删改方法。什么时候用：数据不需要改用元组，比如坐标、RGB 值；需要频繁增删改用列表；函数多值返回用元组是惯例。

---

### Q42：Redis 支持哪些数据类型？各自适用什么场景？

**面试话术**：

"五种基础类型：String 缓存和计数器，Hash 存对象比如用户信息，List 做消息队列或最新列表，Set 做标签和去重，ZSet 做排行榜按分数排序。高级类型：Bitmap 签到统计，HyperLogLog 统计 UV，Geospatial 地理位置，Stream 消息流。我的项目里 String 存商品缓存，ZSet 做热搜榜，List 做异步任务队列。

---

### Q43：MVT 和 MVC 有什么区别？

**面试话术**：

"MVT 是 Django 的架构，跟 MVC 本质一样只是叫法不同。MVC 里 Model 管数据，View 管展示，Controller 管请求分发。MVT 里 Model 一样，Template 对应 View 管展示，View 对应 Controller 管业务逻辑。所以 Django 的 View 其实是 MVC 里的 Controller，Template 是 MVC 里的 View。Django 没有显式 Controller，URL 路由承担了请求分发的职责。

---

### Q42：Django 请求的执行流程是怎样的？

**面试话术**：

"请求先到 Nginx 或 Web 服务器，转发给 Gunicorn。WSGI 把请求封装成 environ，调用 Django。Django 先经过中间件的 process_request，然后 URL 路由匹配到 View。类视图执行 as_view 返回的 dispatch 方法，根据 HTTP 方法分发到 get/post 等。视图处理业务，可能查 Model。返回响应经过中间件 process_response，最后回到浏览器。异常会触发 process_exception。

---

### Q49：Python 怎么实现单例模式？

**面试话术**：

"Python 实现单例有几种方式。第一种重写 `__new__`，判断类有没有 _instance，没有就创建，有就直接返回。第二种用装饰器，维护字典存实例，每次实例化前查字典。第三种用模块导入，Python 模块天然单例，导入一次全局共用。我的项目里数据库连接池用了单例，保证全局只有一个实例，用的是模块级别变量，简单直接。

---

### Q50：Python 变量的作用域有哪些？

**面试话术**：

"Python 作用域按 LEGB 顺序查找：Local 局部作用域，函数内定义，只在函数内有效。Enclosing 嵌套作用域，嵌套函数里内层可访问外层变量，这就是闭包。Global 全局作用域，模块级别，整个模块可访问，函数里用 global 声明才能修改。Builtin 内置作用域，Python 内置的函数和常量。还有 nonlocal 关键字用于修改外层非全局变量。

---

### Q51：生产环境最常用的 10 个 Linux 命令是什么？

**面试话术**：

"查看日志：`tail -f` 实时追踪，配合 `grep` 过滤关键词。查看进程：`ps aux` 或 `top`/`htop` 看实时资源占用。查看端口：`netstat -tulpn` 或 `ss -tulpn` 找进程占用。查看磁盘：`df -h` 看分区，`du -sh` 看目录大小排查磁盘满。网络：`curl` 测试接口，`ping` 测连通，`telnet` 测端口。文件：`find` 查找，`chmod` 改权限，`tar` 打包。还有 `kill` 结束进程，`crontab` 定时任务，`systemctl` 管理服务。

---

### Q52：Python 函数参数是值传递还是引用传递？

**面试话术**：

"Python 既不是纯值传递也不是纯引用传递，叫对象引用传递或共享传参。传的是对象的引用，指向同一个对象。不可变对象如数字、字符串、元组，函数里修改会创建新对象，不影响外面变量。可变对象如列表、字典，函数里修改会影响外面变量，因为是同一个对象。想修改不可变对象或不影响外面变量，需要显式传副本，比如 `list(a)` 或 `a[:]`。

---

### Q53：Python 的垃圾回收机制和内存管理是怎样的？

**面试话术**：

"Python 内存管理分三层：底层 C 的 malloc/free，中间内存池分配小对象，上层垃圾回收。垃圾回收靠引用计数，每个对象有计数器，归零立即回收，实时高效但检测不了循环引用。循环引用靠分代回收解决，维护三个代，回收时检测循环引用并打破。还可以手动 gc.collect() 强制回收，gc.disable() 关闭自动回收。优化内存可以用 `__slots__` 减少对象大小，或用弱引用避免循环引用。

---

### Q54：遇到过内存泄露吗？怎么解决的？

**面试话术**：

"遇到过。Celery 任务处理大量数据时内存一直涨不释放，最后进程被杀掉。排查用 `top` 确认是 Python 进程问题，用 `tracemalloc` 追踪内存分配。发现问题是任务用列表存所有处理结果，数据大了列表占大量内存，而且被其他对象引用没释放。解决方案：改成生成器，处理一条 yield 一条，不用列表存。用 `del` 显式删除大对象，调用 `gc.collect()` 强制回收。还有全局变量缓存了大数据，改成 LRU 缓存限制大小。加了内存监控，超过阈值报警。

---

### Q55：线程池的工作原理是什么？

**面试话术**：

"线程池是预先创建一组线程，任务来了分配给空闲线程执行，执行完不销毁继续等待下一个任务，避免频繁创建销毁线程的开销。Python 里用 `concurrent.futures.ThreadPoolExecutor` 实现，创建时指定最大线程数，提交任务用 `submit` 返回 Future，或用 `map` 批量提交。线程数设置：CPU 密集型设成 CPU 核心数，IO 密集型可以设多一点。注意 Python 的 GIL，多线程对 CPU 密集型任务没有加速效果，这时要用 `ProcessPoolExecutor` 多进程。我的项目网络请求用线程池，计算密集型用进程池。

---

## 附录：Docker 与部署进阶

### Docker 核心概念

**面试话术**：

"Docker 是容器化技术，比虚拟机轻量。虚拟机硬件级虚拟化有完整操作系统，启动慢资源大。Docker 操作系统级虚拟化，共享宿主机内核，启动秒级资源占用小。核心三个概念：镜像只读模板含应用运行所需一切，容器是镜像的运行实例有独立文件系统，仓库存镜像分公共 Docker Hub 和私有。Dockerfile 构建镜像的脚本，FROM 指定基础镜像，COPY 复制文件，RUN 执行命令，CMD/ENTRYPOINT 指定启动命令。

### Docker 网络模式

**面试话术**：

"Docker 五种网络模式。bridge 默认，容器接入 docker0 网桥通过 NAT 访问外网，容器间可互通。host 共享宿主机网络，性能最高但无隔离。container 共享另一容器网络。none 无网络。overlay 跨主机网络用于集群。我的项目用 bridge 模式，多容器在同一网络通过容器名互相访问，比如 Django 访问 Redis 直接用 redis://redis:6379。

### Docker 镜像优化

**面试话术**：

"优化镜像体积：选 alpine 精简基础镜像；合并 RUN 指令减少层数；清理构建缓存；用多阶段构建分离构建和运行环境；.dockerignore 排除 .git、node_modules、日志等无用文件；只安装运行必须的依赖，不装开发调试工具。

### Docker Compose 部署 Django

**面试话术**：

"我的项目用 Docker Compose 部署，docker-compose.yml 定义 Django、Nginx、Redis、PostgreSQL 四个服务。Django 服务用 Gunicorn 启动，Gunicorn 配置 worker 数量和超时时间。Nginx 反向代理到 Gunicorn，处理静态文件和 SSL。Redis 和 PostgreSQL 用官方镜像。启动命令 docker-compose up -d，数据库迁移 docker-compose exec django python manage.py migrate。

---

## 附录：PostgreSQL 进阶

### PostgreSQL 索引类型

**面试话术**：

"PostgreSQL 支持多种索引。B 树索引是默认，适用等值查询和范围查询。GIN 索引是倒排索引，适用数组、JSONB、全文搜索。GiST 索引适用空间数据和几何类型。BRIN 索引适用有序连续的超大表比如时间序列，占用空间极小。哈希索引仅适用等值查询，不支持范围和排序。我的项目里外键用 B 树，JSONB 字段用 GIN 索引，时间字段用 BRIN 索引。

### PostgreSQL JSON 和 JSONB 的区别

**面试话术**：

"JSON 是文本格式存储，插入快但查询慢，不支持索引。JSONB 是二进制格式存储，插入时需解析稍慢，但查询快且支持 GIN 索引，不保留格式和键顺序自动去重重复键。绝大多数场景用 JSONB，特别是需要对 JSON 内字段查询、过滤、索引的场景。JSON 仅用于仅需完整存储无需查询的归档场景。

### PostgreSQL 慢查询优化

**面试话术**：

"慢查询优化流程：开启慢查询日志设 log_min_duration_statement=100ms；用 pgBadger 分析定位高频高耗时 SQL；用 EXPLAIN ANALYZE 查看执行计划找瓶颈：是否全表扫描、索引是否生效、JOIN 类型是否合理、是否有磁盘排序。优化手段：加合适的索引避免全表扫描；避免 SELECT *；优化大偏移量分页；执行 ANALYZE 更新统计信息；调整 shared_buffers、work_mem 等核心参数。

### PostgreSQL 表分区

**面试话术**：

"表分区把大逻辑表拆成多个物理分区表，提升查询性能和维护效率。范围分区按字段范围分区，最常用按时间分区。列表分区按枚举值分区，比如按地区或订单状态。哈希分区按哈希值取模，数据均匀分布。适用场景：数据量超千万亿级查询下降；有明显冷热区分只访问近期数据；需定期删除历史数据可直接 DROP 分区，避免大量 DELETE。

---

## 补充面试题 Q56-Q90（深度防守层）

### Q56：Python 的异常处理机制是怎样的？

**面试话术**：

"Python 异常处理用 try-except-finally 结构。try 块放可能出错的代码，except 捕获特定异常，可以多个 except 按从具体到宽泛的顺序。finally 块无论是否异常都会执行，常用于资源释放。else 块在没异常时执行。还可以用 raise 主动抛异常，自定义异常继承 Exception 类。项目中我用 try-except 包裹第三方 API 调用，防止网络超时导致整个请求失败。"

**要点**：try-except-else-finally、异常捕获顺序、自定义异常、raise

---

### Q57：什么是上下文管理器？with 语句的原理？

**面试话术**：

"上下文管理器是实现了 __enter__ 和 __exit__ 方法的对象，with 语句自动调用它们。__enter__ 进入时执行返回资源，__exit__ 退出时清理资源，即使有异常也能保证释放。最常用的是文件操作 with open()，自动关闭文件。项目中我自己写过 Redis 连接的上下文管理器，确保连接用完后归还连接池。也可以用 contextlib 装饰器快速创建。"

**要点**：__enter__/__exit__、资源自动释放、contextlib、使用场景

---

### Q43：Python 常用魔法方法有哪些？

**面试话术**：

"魔法方法是双下划线开头结尾的特殊方法。__init__ 初始化对象，__new__ 创建实例，__str__ 和 __repr__ 控制打印输出，__call__ 让对象可调用，__getitem__/__setitem__ 支持索引操作，__iter__/__next__ 让对象可迭代，__enter__/__exit__ 是上下文管理器。还有比较方法 __eq__、__lt__ 等，算术方法 __add__、__mul__ 等。项目中我用 __str__ 优化模型对象的日志输出。"

**要点**：初始化、字符串表示、可调用、可迭代、上下文管理、运算符重载

---

### Q59：什么是 Mixin 模式？

**面试话术**：

"Mixin 是一种多继承设计模式，把特定功能封装成独立类，其他类通过继承来复用。Mixin 类本身不单独使用，只提供方法不定义实例属性。Django 里很多 Mixin，比如 LoginRequiredMixin 要求登录、PermissionRequiredMixin 检查权限。项目中我用 Mixin 封装了分页逻辑和 API 响应格式，多个视图类直接继承复用，避免重复代码。"

**要点**：多继承、功能复用、Django Mixin、与基类区别

---

### Q43：什么是 CSRF 攻击？Django 怎么防护？

**面试话术**：

"CSRF 是跨站请求伪造，攻击者诱导用户在已登录状态下访问恶意链接，以用户身份执行非预期操作。比如用户登录银行后访问攻击者网站，自动发起转账请求。Django 防护机制：模板表单里加 {% csrf_token %}，生成随机 token 存 session，提交时校验。中间件 CsrfViewMiddleware 自动处理。对于 API 用 @csrf_exempt 豁免，配合 Token/JWT 认证。项目中所有表单都加了 csrf_token，API 用 DRF 的认证替代。"

**要点**：攻击原理、token 机制、CsrfViewMiddleware、API 豁免

---

### Q42：Django ORM 怎么做聚合查询？

**面试话术**：

"Django ORM 用 aggregate 和 annotate 做聚合。aggregate 对整个 QuerySet 聚合返回字典，比如 Order.objects.aggregate(Sum('amount')) 返回总金额。annotate 给每个对象添加聚合字段，比如给每个用户 annotate 订单数量。常用聚合函数 Sum、Count、Avg、Max、Min。还可以配合 values 分组聚合。项目中我用 annotate 统计每个商品的销售量，用 aggregate 算订单总额。"

**要点**：aggregate vs annotate、聚合函数、分组聚合、使用场景

---

### Q43：Django 怎么实现读写分离？

**面试话术**：

"Django 支持多数据库配置，DATABASES 里配置 default 和 replica。写操作走 default，读操作走 replica。实现方式：1）手动指定 using('replica')；2）写数据库路由类，继承 BaseDatabaseRouter，定义 db_for_read 返回 replica，db_for_write 返回 default；3）用装饰器或上下文管理器动态切换。项目中我配了主从复制，用路由类自动分配，查询走从库减轻主库压力。"

**要点**：多数据库配置、Database Router、主从复制、自动路由

---

### Q42：怎么写 Django 自定义管理命令？

**面试话术**：

"Django 自定义命令在应用下建 management/commands 目录，写 Python 文件继承 BaseCommand。定义 handle 方法写逻辑，用 add_arguments 加参数。命令名就是文件名，执行 python manage.py 命令名。项目中我写了清理过期订单的命令，配 crontab 定时执行。还写了数据导入导出命令，方便运营批量操作。"

**要点**：目录结构、BaseCommand、handle 方法、参数解析、定时任务

---

### Q43：Django 缓存怎么设置过期时间和版本控制？

**面试话术**：

"Django 缓存用 cache.set(key, value, timeout) 设过期时间，单位秒。timeout=None 永不过期，timeout=0 不缓存。版本控制用 cache.add 只在 key 不存在时设值，或用 cache.incr/decr 原子增减。也可以用不同的 key 前缀做版本隔离，比如 v1:products、v2:products。项目中商品列表缓存设 5 分钟过期，用户购物车用版本号管理，清空购物车时直接改版本号让旧缓存失效。"

**要点**：timeout 参数、cache.add、原子操作、版本号策略、缓存失效

---

### Q65：数据库事务隔离级别有哪些？各有什么问题？

**面试话术**：

"四个隔离级别：READ UNCOMMITTED 读未提交，可能脏读；READ COMMITTED 读已提交，避免脏读但可能不可重复读；REPEATABLE READ 可重复读，避免不可重复读但可能幻读；SERIALIZABLE 串行化，完全隔离但性能最差。Django 默认读已提交，用 transaction.atomic 开启事务。脏读是读到未提交数据，不可重复读是同一事务两次读结果不同，幻读是同一事务两次查询行数不同。项目中库存扣减用可重复读防止超卖。"

**要点**：四个级别、脏读/不可重复读/幻读、Django 默认级别、使用场景

---

### Q66：什么是数据库连接池？

**面试话术**：

"连接池是预先创建好数据库连接放在池子里，用的时候取，用完归还，避免频繁创建销毁连接的开销。Django 默认用连接池，PostgreSQL 用 psycopg2 的连接池。配置 CONN_MAX_AGE 控制连接复用时间。连接池大小要平衡，太小并发不够，太大占用资源。项目中我设 CONN_MAX_AGE=600 让连接复用 10 分钟，减少连接开销。"

**要点**：连接复用、CONN_MAX_AGE、池大小配置、性能优化

---

### Q42：Session、Cookie 和 Token 有什么区别？

**面试话术**：

"Cookie 是浏览器存储的小型文本，随请求自动携带，可设过期时间和作用域。Session 是服务器端存储，用 Cookie 存 sessionid 关联，数据存在服务端更安全。Token 是无状态认证，服务器不存储，客户端每次带 token 来验签，常用 JWT。Cookie/Session 有状态，适合传统 Web；Token 无状态，适合 API 和移动端。项目中 Web 用 Session，APP 接口用 JWT Token。"

**要点**：存储位置、安全性、有状态 vs 无状态、适用场景

---

### Q43：JWT 是什么？结构是怎样的？

**面试话术**：

"JWT 是 JSON Web Token，轻量级认证方式。结构分三部分：Header 声明类型和算法，Payload 存用户数据，Signature 是签名防篡改。三部分用点号连接，Base64Url 编码。优点是无状态、跨域友好、适合分布式。缺点是 token 无法主动失效、Payload 可解码别放敏感信息。项目中用户登录后返回 JWT，后续请求带 Authorization: Bearer token 头。"

**要点**：Header/Payload/Signature、无状态认证、优缺点、使用方式

---

### Q42：怎么防止 XSS 和 SQL 注入？

**面试话术**：

"XSS 是跨站脚本攻击，注入恶意脚本。防护：转义用户输入，Django 模板自动转义 {{ var }}；设 CSP 内容安全策略；Cookie 加 HttpOnly 防止脚本读取。SQL 注入是通过输入篡改 SQL。防护：用 ORM 参数化查询，Django ORM 自动转义；原生 SQL 用参数占位符 %s，别用字符串拼接；限制数据库权限。项目中我全用 ORM，模板输出都转义，Cookie 设 HttpOnly。"

**要点**：XSS 防护（转义、CSP、HttpOnly）、SQL 注入防护（ORM、参数化）

---

### Q43：什么是工厂模式？

**面试话术**：

"工厂模式是创建型设计模式，把对象创建逻辑封装起来，调用者不用关心具体类。简单工厂用一个工厂类根据参数创建不同对象；工厂方法把创建延迟到子类；抽象工厂创建相关对象族。项目中我用工厂模式封装支付方式创建，PayFactory.create('alipay') 返回支付宝对象，create('wechat') 返回微信支付对象，新增支付方式只需扩展工厂，不影响调用代码。"

**要点**：简单工厂/工厂方法/抽象工厂、解耦创建逻辑、扩展性

---

### Q42：项目做过哪些性能优化？

**面试话术**：

"我的项目做了这些优化：1）数据库加索引，外键和查询字段都建了索引；2）Redis 缓存热点数据，商品列表、用户信息都缓存；3）Django ORM 用 select_related、prefetch_related 避免 N+1；4）Celery 异步处理邮件发送，不阻塞主请求；5）数据库读写分离，查询走从库；6）静态文件 CDN 加速；7）Gunicorn 多 worker 处理并发。优化后接口响应从几百毫秒降到几十毫秒。"

**要点**：数据库优化、缓存、异步、读写分离、CDN、并发处理

---

### Q72：遇到过线上故障吗？怎么排查的？

**面试话术**：

"遇到过 Redis 连接数打满导致请求超时。排查过程：先看监控告警确定是 Redis 问题，登录服务器用 redis-cli info 看连接数，发现接近上限；查代码发现有个循环里每次迭代都新建连接没关闭；用 lsof 和 netstat 确认连接泄露；修复代码加连接池管理，重启服务。事后加连接数监控告警，代码 review 加检查点。还配了 Sentry 捕获异常，日志用 ELK 方便检索。"

**要点**：故障案例、排查步骤、监控工具、复盘改进

---

### Q42：项目怎么做日志和监控？

**面试话术**：

"日志用 loguru 记录，分级别 INFO、WARNING、ERROR，按天切割。Django 中间件记录请求耗时和异常，Celery 任务记录执行日志。日志文件用 Filebeat 收集到 Elasticsearch，Kibana 可视化查询。监控用 Prometheus + Grafana，Django 配 django-prometheus 暴露指标，监控 QPS、响应时间、错误率。还配了 Sentry 实时告警异常，微信机器人通知。数据库用 pg_stat_statements 监控慢查询。"

**要点**：日志框架、日志收集、监控指标、告警机制

---

### Q74：Python 多进程和多线程怎么选？

**面试话术**：

"IO 密集型选多线程，比如网络请求、文件读写，线程遇到 IO 会释放 GIL，其他线程能执行。CPU 密集型选多进程，多核并行绕过 GIL，真正利用多核。也可以用多进程 + 多线程混合。项目中图片处理是 CPU 密集型用多进程，API 请求聚合用多线程。还可以用 asyncio 做异步 IO，单线程协程处理高并发。"

**要点**：IO 密集 vs CPU 密集、GIL 影响、asyncio 替代方案

---

### Q42：什么是异步 IO？asyncio 的原理？

**面试话术**：

"异步 IO 是非阻塞 IO 编程，单线程处理大量并发连接。asyncio 是 Python 标准库，核心概念：event loop 事件循环调度任务；coroutine 协程用 async def 定义；await 挂起协程让出控制权；Future/Task 包装协程。原理是遇到 IO 操作时挂起当前任务，去执行其他任务，IO 完成再恢复。项目中我用 asyncio + aiohttp 做并发 API 调用，比同步请求快几倍。Django 3.1+ 也支持 ASGI 异步视图。"

**要点**：event loop、coroutine、await、非阻塞 IO、适用场景

---

### Q43：支付宝支付的 RSA2 签名流程是怎样的？

**面试话术**：

"支付宝用 RSA2 非对称加密做签名验签。流程：商家生成密钥对，公钥给支付宝配置，私钥自己保管。下单时商家用私钥对订单参数签名，支付宝用商家公钥验签确认请求来源。支付回调时支付宝用支付宝私钥签名，商家用支付宝公钥验签确认回调真实性。项目中我用 alipay-sdk-python，配置 app_id、私钥、支付宝公钥，sdk 自动处理签名验签，我只需关注业务逻辑。"

**要点**：非对称加密、私钥签名公钥验签、密钥配置、sdk 封装

---

### Q42：怎么保证支付订单的幂等性？

**面试话术**：

"幂等性是指同一请求多次执行结果相同。支付订单用订单号做幂等键，数据库设唯一索引防止重复插入。流程：用户点击支付生成唯一订单号存数据库状态待支付；调支付宝前检查订单状态，已支付直接返回成功；支付宝回调时同样用订单号幂等，已处理过的直接返回 success。Redis 加分布式锁防止并发重复提交，锁 key 用订单号，保证同一订单同时只有一个处理流程。"

**要点**：唯一订单号、数据库唯一索引、状态机、分布式锁

---

### Q43：支付宝的同步回调和异步回调有什么区别？

**面试话术**：

"同步回调是支付完成后支付宝跳转回商家页面，用 GET 请求带参数，给用户展示支付结果。但用户可能中途关闭页面，所以不可靠。异步回调是支付宝服务器主动发 POST 请求到商家服务器通知支付结果，更可靠必须处理。项目中同步回调只做页面展示，真正的订单状态更新在异步回调里处理，异步回调返回 success 字符串告诉支付宝处理成功，否则会重试。"

**要点**：同步跳转展示、异步通知更新、可靠性差异、重试机制

---

### Q79：怎么防止重复支付？

**面试话术**：

"重复支付防护多层：前端按钮点击后禁用防止连点；后端接口加 Redis 分布式锁，同一订单同一时间只能有一个支付流程；数据库订单表加唯一索引，同一业务订单号只能有一条记录；支付前检查订单状态，已支付直接返回；支付宝侧也做幂等，同一订单号多次请求返回同一结果。万一重复支付走退款流程或充余额。"

**要点**：前端防抖、分布式锁、唯一索引、状态检查、退款兜底

---

### Q80：Celery 怎么做任务路由和优先级队列？

**面试话术**：

"Celery 任务路由用 task_routes 配置，按任务名或正则匹配到指定队列。比如邮件任务路由到 email_queue，订单任务路由到 order_queue。优先级队列用 RabbitMQ 或 Redis 做 broker，配置 task_priority 支持 0-9 级优先级，数字越小优先级越高。项目中邮件发送优先级低设 5，订单超时取消优先级高设 1。不同队列可以配不同 worker 消费，实现资源隔离。"

**要点**：task_routes 配置、队列隔离、优先级设置、worker 分配

---

### Q42：怎么监控 Celery 任务的执行情况？

**面试话术**：

"Celery 监控几种方式：Flower 是官方监控工具，Web 界面看任务状态、执行时间、worker 负载；Django 管理后台看任务结果表；日志记录任务开始结束和异常；Prometheus + Grafana 用 celery-exporter 采集指标。项目中我用 Flower 看实时任务流，配了 Prometheus 监控任务成功率和平均执行时间，异常任务发钉钉告警。"

**要点**：Flower 监控、日志追踪、Prometheus 指标、告警机制

---

### Q43：什么是死信队列？怎么配置？

**面试话术**：

"死信队列 DLX 是处理失败任务的机制，任务重试次数超限或直接被拒绝时进死信队列，避免阻塞正常队列。Celery 配 task_acks_late=True 和 task_reject_on_worker_lost=True，设置 max_retries 重试次数。项目中我配了 3 次重试，间隔指数退避 1 分钟、2 分钟、4 分钟，都失败后进死信队列，人工处理或发告警。"

**要点**：DLX 概念、重试配置、指数退避、人工介入

---

### Q83：RESTful API 的设计规范有哪些？

**面试话术**：

"RESTful 设计规范：用 HTTP 方法表示操作，GET 查、POST 建、PUT 全量更新、PATCH 局部更新、DELETE 删；URL 用名词复数，比如 /orders、/users/{id}；状态码规范，200 成功、201 创建、204 无内容、400 参数错误、401 未认证、403 无权限、404 不存在、500 服务器错；返回统一格式，包含 code、message、data；支持过滤、分页、排序。项目中我严格按规范设计，配合 DRF 自动生成文档。"

**要点**：HTTP 方法语义、URL 命名、状态码规范、统一返回格式

---

### Q84：API 版本控制有哪些方案？

**面试话术**：

"API 版本控制几种方案：URL 路径版本，比如 /v1/users、/v2/users，最直观；请求头版本，比如 Accept: application/vnd.api.v1+json，更 RESTful；查询参数版本，比如 ?version=1.0，简单但不够规范。项目中我用 URL 路径版本，v1 是当前稳定版，v2 开发新功能，兼容期内同时维护，逐步迁移。"

**要点**：URL 路径、请求头、查询参数、兼容迁移

---

### Q42：Django 怎么做单元测试和接口测试？

**面试话术**：

"Django 测试继承 TestCase，用 setUp 初始化数据，写 test_ 开头的方法。测试数据库是隔离的，每次测试完自动回滚。接口测试用 Django 的 Client 或 DRF 的 APIClient，模拟 GET、POST 请求，断言响应状态码和数据。项目中我写测试覆盖模型方法、视图逻辑、序列化器，用 coverage 检查覆盖率，CI 流程里跑测试必须全过才能合并。"

**要点**：TestCase、setUp/tearDown、APIClient、数据库隔离、coverage

---

### Q43：什么是 Mock？测试中怎么用？

**面试话术**：

"Mock 是模拟对象，替代真实依赖，控制返回值验证调用。Python 用 unittest.mock 模块，patch 装饰器替换函数或类。测试中 Mock 外部 API 调用，比如支付宝支付接口，不用真调支付宝，Mock 返回成功失败各种情况。项目中我 Mock 短信发送、邮件发送、第三方支付，测试各种边界和异常分支，保证代码健壮。"

**要点**：Mock 概念、unittest.mock、patch 装饰器、外部依赖隔离

---

### Q42：Git 常用工作流有哪些？你们用什么？

**面试话术**：

"常用 Git 工作流：Git Flow 分 master、develop、feature、release、hotfix 分支，适合版本发布；GitHub Flow 简单，master 直接部署，feature 分支开发完 PR 合并，适合持续部署；GitLab Flow 加 pre-production、production 环境分支。我们小团队用简化版 Git Flow，master 稳定，dev 开发，feature 分支开发功能，提 PR 到 dev，测试完合并 master 打 tag 发布。"

**要点**：Git Flow、GitHub Flow、分支策略、PR 流程

---

### Q43：Git rebase 和 merge 有什么区别？

**面试话术**：

"merge 把分支合并，保留完整历史，产生合并提交，历史有分叉。rebase 把当前分支提交变基到目标分支最新提交上，历史变成线性，更整洁但改写历史。公共分支用 merge 保留历史，个人 feature 分支用 rebase 整理提交再合并。项目中 feature 分支开发完我先用 rebase -i  squash 整理提交，再 rebase dev 解决冲突，最后 merge --no-ff 到 dev。"

**要点**：历史差异、线性 vs 分叉、使用场景、黄金法则

---

### Q89：Git 冲突怎么解决？

**面试话术**：

"冲突发生在同一文件同一位置被不同修改。解决：git status 看冲突文件，打开文件找 <<<<<<< ======= >>>>>>> 标记，手动选择保留哪部分或合并，保存后 git add 标记解决，git commit 完成合并。预防：开发前 git pull 更新，小步提交，和队友沟通避免同时改同一文件。复杂冲突用 IDE 图形化工具更方便。"

**要点**：冲突标记、手动解决、add/commit、预防措施

---

### Q90：代码审查（Code Review）看哪些方面？

**面试话术**：

"Code Review 看这几方面：功能正确性，是否满足需求；代码规范，命名、格式、注释；设计合理性，是否符合设计模式、是否过度设计；性能问题，N+1 查询、循环里查库；安全隐患，SQL 注入、XSS、敏感信息泄露；测试覆盖，是否有测试、边界情况；可读性，逻辑清晰、复杂度适中。项目中我们用 GitHub PR 做 review，至少一人 approve 才能合并。"

**要点**：功能、规范、设计、性能、安全、测试、可读性

---

**祝面试顺利！记住，自信 + 真实 + 有思考 = 好印象。**

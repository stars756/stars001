# ============================================================
# BaykeShop Nginx 反向代理配置（生产环境）
# 位置: docker/nginx/nginx.conf
#
# 功能:
#   - HTTPS 终端（需挂载 SSL 证书到 /etc/nginx/certs/）
#   - 静态文件直接由 Nginx 提供（不走 Django）
#   - 媒体文件服务
#   - API 请求反向代理到 Gunicorn
#   - Gzip 压缩
#   - 安全头
# ============================================================

upstream django_backend {
    server web:8000;
    keepalive 32;
}

server {
    listen 80;
    server_name _;
    # 强制 HTTPS（生产环境取消注释）
    # return 301 https://$host$request_uri;

    # 开发环境可直接用 HTTP
    location / {
        proxy_pass http://django_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # WebSocket 支持（如需 Django Channels）
        # proxy_set_header Upgrade $http_upgrade;
        # proxy_set_header Connection "upgrade";

        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }

    # 静态文件 — 直接由 Nginx 提供，不经过 Django
    location /static/ {
        alias /usr/share/nginx/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # 媒体文件（用户上传的图片等）
    location /media/ {
        alias /usr/share/nginx/media/;
        expires 7d;
        add_header Cache-Control "public";
        access_log off;
    }

    # Swagger API 文档
    location /api/docs/ {
        proxy_pass http://django_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ReDoc 文档
    location /api/redoc/ {
        proxy_pass http://django_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 健康检查端点
    location /api/schema/ {
        proxy_pass http://django_backend;
        proxy_set_header Host $host;
        access_log off;
    }
}

# HTTPS 配置（生产环境使用，需要 SSL 证书）
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#     ssl_protocols       TLSv1.2 TLSv1.3;
#     ssl_ciphers         ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256;
#     ssl_prefer_server_ciphers on;
#     ssl_session_cache   shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     # Gzip 压缩
#     gzip on;
#     gzip_vary on;
#     gzip_proxied any;
#     gzip_comp_level 6;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/rss+xml image/svg+xml;
#
#     # 安全头
#     add_header X-Frame-Options "SAMEORIGIN" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-XSS-Protection "1; mode=block" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # ... 其余配置同上 ...
# }
